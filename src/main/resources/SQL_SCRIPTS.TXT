CREATE DATABASE JOB4J_DATABASE
CREATE TABLE DB_USERS (
ID SERIAL PRIMARY KEY,
UserName CHARACTER VARYING(30),
Age INTEGER,
hasFullAccess bool
);
INSERT INTO
DB_USERS (USERNAME, AGE, HASFULLACCESS)
VALUES ('Vlasov Pavel', 22, true)
UPDATE DB_USERS
SET USERNAME = 'Pavel Vlasov';
DELETE FROM DB_USERS